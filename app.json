{
  "name": "letsdevtech-proxy-squid",
  "description": "Container based on another repo. =D",
  "stack": "container",
  "env": {
    "USERNAME": {
      "description": "Username for authentication or not....",
      "required": false
    },
    "PASSWORD": {
      "description": "Password for authentication or not....",
      "required": false
    }
  }
}
